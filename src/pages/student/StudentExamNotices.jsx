import React from 'react';
import NoticeList from '../../components/NoticeList';

const StudentExamNotices = () => {
  return <NoticeList collectionName="exam_notices" title="Exam Notices" />;
};

export default StudentExamNotices;
